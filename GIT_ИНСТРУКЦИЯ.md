# Инструкция по работе с Git и GitHub

## Репозиторий уже инициализирован ✅
- Git репозиторий создан
- Первый коммит сделан (33 файла)

## Как загрузить на GitHub:

### 1. Создай репозиторий на GitHub
- Зайди на https://github.com
- Нажми "New repository" (зелёная кнопка)
- Название: `SubtitleReader` (или любое другое)
- Описание: `WPF приложение для чтения игровых субтитров с OCR и TTS`
- **НЕ** создавай README, .gitignore или лицензию (они уже есть)
- Нажми "Create repository"

### 2. Подключи удалённый репозиторий
**ВАЖНО:** Команды нужно запускать из папки `SubtitleReader\SubtitleReader` (там где .git)!

```bash
cd C:\Users\pavel\Desktop\SubtitleReader\SubtitleReader
git remote add origin https://github.com/ТвойЛогин/SubtitleReader.git
git branch -M main
git push -u origin main
```

Замени `ТвойЛогин` на свой логин GitHub!

**Или короче (если уже в папке SubtitleReader):**
```bash
cd SubtitleReader
git remote add origin https://github.com/ТвойЛогин/SubtitleReader.git
git branch -M main
git push -u origin main
```

### 3. Готово!
Твой проект теперь на GitHub 🎉

## Полезные команды Git:

### Проверить статус
```bash
git status
```

### Добавить изменения
```bash
git add .
```

### Сделать коммит
```bash
git commit -m "Описание изменений"
```

### Отправить на GitHub
```bash
git push
```

### Посмотреть историю
```bash
git log --oneline
```

### Создать новую ветку
```bash
git checkout -b название-ветки
```

## Что уже в репозитории:
- ✅ Весь исходный код (Models, Services, Views, ViewModels)
- ✅ XAML файлы интерфейса
- ✅ Файл проекта (.csproj)
- ✅ Документация (README, инструкции на русском)
- ✅ .gitignore (исключает bin/, obj/, временные файлы)

## Что НЕ в репозитории (правильно):
- ❌ bin/ и obj/ (скомпилированные файлы)
- ❌ .vs/ (настройки Visual Studio)
- ❌ Большие файлы tessdata/*.traineddata
- ❌ Временные файлы

Это правильно! Эти файлы генерируются автоматически при сборке.
