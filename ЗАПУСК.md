# 🚀 Как запустить SubtitleReader

## Быстрый старт

### 1. Найдите EXE файл

После сборки проекта, исполняемый файл находится здесь:
```
SubtitleReader/bin/Release/net9.0-windows/SubtitleReader.exe
```

Или для Debug версии:
```
SubtitleReader/bin/Debug/net9.0-windows/SubtitleReader.exe
```

### 2. Установите языковые файлы для OCR (ОБЯЗАТЕЛЬНО!)

Для работы распознавания текста нужны языковые файлы Tesseract:

**Скачайте файлы:**
- Русский: https://github.com/tesseract-ocr/tessdata/raw/main/rus.traineddata
- Английский: https://github.com/tesseract-ocr/tessdata/raw/main/eng.traineddata

**Поместите их в папку:**
```
SubtitleReader/bin/Release/net9.0-windows/tessdata/
```

Структура должна быть такой:
```
SubtitleReader.exe
tessdata/
  rus.traineddata
  eng.traineddata
```

### 3. Запустите программу

Просто дважды кликните на `SubtitleReader.exe`

Или через командную строку:
```cmd
cd SubtitleReader\bin\Release\net9.0-windows
SubtitleReader.exe
```

## Сборка проекта

### Debug версия (для разработки):
```cmd
cd SubtitleReader
dotnet build
```

### Release версия (оптимизированная):
```cmd
cd SubtitleReader
dotnet build -c Release
```

### Запуск без сборки:
```cmd
cd SubtitleReader
dotnet run
```

## Создание портативной версии

Чтобы создать версию, которую можно запускать на любом компьютере без установки .NET:

```cmd
cd SubtitleReader
dotnet publish -c Release -r win-x64 --self-contained -p:PublishSingleFile=true
```

Результат будет в:
```
SubtitleReader/bin/Release/net9.0-windows/win-x64/publish/
```

## Требования

- **Windows 10/11** (64-bit)
- **.NET 9.0 Runtime** (если не используете self-contained версию)
- **Языковые файлы Tesseract** в папке tessdata

## Первый запуск

1. Запустите `SubtitleReader.exe`
2. Нажмите **"🎯 Выделить область на экране"**
3. Выделите область с текстом (например, откройте любой текст и выделите его)
4. Текст будет автоматически распознан
5. Нажмите **"▶ Читать"** чтобы озвучить текст

## Возможные проблемы

### "OCR недоступен"
- Проверьте наличие файлов `rus.traineddata` и `eng.traineddata` в папке `tessdata`
- Убедитесь что файлы не повреждены (размер rus.traineddata ~1.5 MB)

### "Не удалось найти .NET Runtime"
- Установите .NET 9.0 Runtime: https://dotnet.microsoft.com/download/dotnet/9.0
- Или используйте self-contained версию (см. выше)

### Программа не запускается
- Проверьте что у вас Windows 10/11
- Запустите от имени администратора
- Проверьте антивирус (может блокировать)

## Горячие клавиши (настраиваются)

По умолчанию:
- **F9** - Начать мониторинг всех областей
- **F10** - Остановить мониторинг
- **F11** - Прочитать выбранную область

Изменить можно в **⚙️ Настройки**

## Полезные ссылки

- Языковые файлы Tesseract: https://github.com/tesseract-ocr/tessdata
- .NET 9.0 Download: https://dotnet.microsoft.com/download/dotnet/9.0
- Документация проекта: README.md
